void processQueries(int processRank, long int* queryNumList, long int* queriesProcessX, long int* queriesProcessY, long int numQueries, long int* unionfindDs, int* pointIdMapping, long int numPointsPerProcess, int numProcesses)
{
	int arrIndex = 0;
	int i;
	int finished[numProcesses];
	for(i = 0; i < numProcesses; i++)
	{
		finished[i] = 0;
	}
	for(arrIndex = 0; arrIndex < numQueries; arrIndex++)
	{

	}
}